const handleCreateUser = (username: string, email: string, address: string) => {
                // hiển thị thông tin người dùng
                console.log(`User created: ${username}, Email: ${email}, Address: ${address}`);
};
export default handleCreateUser;